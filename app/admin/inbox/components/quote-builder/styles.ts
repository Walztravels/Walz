// QUOTE BUILDER V1.2 — shared style tokens, lifted verbatim from the
// original CreateQuoteDrawer.tsx inline consts so every new desktop/tablet/
// mobile component matches the existing Walz admin visual language instead
// of each agent inventing its own.
export const inputCls = 'w-full min-h-[44px] px-3 py-2 rounded-lg border border-walz-border bg-white text-sm text-walz-deep-navy focus:outline-none focus:ring-2 focus:ring-walz-gold/60'
export const labelCls = 'block text-[10px] font-bold text-walz-muted-strong uppercase tracking-widest mb-1'
