'use client'

// Placeholder — auto-assign settings are shown inside StaffModal's Auto-Assign tab.
// This component is reserved for a standalone settings page if needed.
export function AutoAssignSettings() {
  return null
}
